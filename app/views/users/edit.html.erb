<%= simple_form_for @user do |f| %>
  <%= f.file_field :profile_photo %>
  <%= f.input :profile_photo, :as => :image_preview, :input_html => {:preview_version => :thumb} %>
  <%= f.button :submit %>
<% end %>
